import styled from 'styled-components';

import {secondaryFontColor, weightExtraLight} from '../../styles/variables';

export default styled.span`
  color: ${secondaryFontColor};
  font-weight: ${weightExtraLight};
  margin-top: 4px;
`;
