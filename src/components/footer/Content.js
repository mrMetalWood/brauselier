import styled from 'styled-components';

import {mainMaxWidth} from '../../styles/variables';

export default styled.div`
  align-items: center;
  display: flex;
  height: 100%;
  justify-content: center;
  margin: 0 auto;
  max-width: ${mainMaxWidth};
  width: 100%;
`;
